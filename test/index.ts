// import { KeyManager, KeyManagerConfig } from '../src/class'


// const config: { [key: string]: KeyManagerConfig } = { jwt: { keyAmount: 5, rotation: 1 } };

// const manager = new KeyManager("test", [], config);


// (async () => {
//     // manager.initCreationString()
//     // await manager.generateInitKeys()
//     await manager.generateInitKeys().then(async () => {
//         console.log("result", await manager.getKeys("jwt", 0))
//     })

//     //await manager.syncKeys()

// })()